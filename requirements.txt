# Core app
streamlit>=1.38.0

# Tab 1: Gap Cutter -- unchanged dependency
pydub>=0.25.1

# Tab 2: Video Enhancer uses ffmpeg directly via subprocess -- no extra Python
# package needed, just the ffmpeg binary from packages.txt